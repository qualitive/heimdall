ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.qualitive.ApplicationKt.module ]
    }
}

heimdall {
    url = "http://localhost:8080"
    successRedirect = "http://localhost:8080/success"
    failureRedirect = "http://localhost:8080/failure"

    google {
        appId = test
        secret = test
    }

    msid {
        appId = test
        secret = test
        tenant = test
    }
}

keystore {
    keystorePass = KeyStore
    privateKeyPass = PrivateKey
}
